---
description: Configure the specraft server URL and API key, writing .specraft.json and storing credentials privately
---

# /specraft-setup

Ask for the specraft server URL and API key. Write `.specraft.json` with `server_url` and `strict_mode: true`, and store the key in the user's private environment or `~/.specraft/credentials`.
