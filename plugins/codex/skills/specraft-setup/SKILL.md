---
name: specraft-setup
description: Configure this repository to use a specraft server and API key.
---

# specraft-setup

Ask for the specraft server URL and API key. Write `.specraft.json` with `server_url` and `strict_mode: true`, and store the API key outside git in the user's private environment or `~/.specraft/credentials`.
